# Part 2 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix documentation score staleness and missing endpoint disclosures, and add a test for literal backslashes in keyword search.

**Architecture:** Documentation updates + single integration test addition following existing patterns.

**Tech Stack:** Markdown, Python, Pytest, SQLAlchemy.

**Spec:** N/A (User requested targeted fixes)

## Global Constraints

- No semantic/vector retrieval implementation.
- No new endpoint implementations.

## Review Focus

- Literal backslash escaping handles `\` vs `\%` correctly.

---

### Task 1: Update API Documentation

**Files:**
- Modify: `docs/api/CHAT_SEARCH_API.md:40-80`

**Interfaces:**
- N/A

- [ ] **Step 1: Update score in Search Response**
  Change `"score": 0.95` to `"score": 1.0` and add note about future semantic scoring.

- [ ] **Step 2: Add status note to endpoints**
  Add "*Not yet implemented — documented here as the target contract for a future phase*" to sections 2, 3, 4, and 5.

### Task 2: Add Literal Backslash Test

**Files:**
- Modify: `server/tests/services/test_retrieval.py:315-330`

**Interfaces:**
- N/A

- [ ] **Step 1: Write test_literal_backslash**
  Add a new method `test_literal_backslash` below `test_literal_underscore`.

- [ ] **Step 2: Run test to verify it passes**
  Run: `pytest tests/services/test_retrieval.py::TestKeywordRetrievalStrategyIntegration::test_literal_backslash -v`
  Expected: PASS
